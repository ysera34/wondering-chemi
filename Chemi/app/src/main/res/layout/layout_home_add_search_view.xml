<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/home_add_search_layout"
    android:layout_width="match_parent"
    android:layout_height="@dimen/search_editText_height"
    android:background="@color/colorWhite">

    <Button
        android:id="@+id/home_add_search_text_button"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentStart="true"
        android:layout_toStartOf="@+id/home_add_search_image_button"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:textSize="15sp"
        android:textAlignment="textStart"
        android:textColorHint="@color/colorFontHint"
        android:ellipsize="end"
        android:maxLines="1"
        android:background="@android:color/transparent"/>

    <ImageButton
        android:id="@+id/home_add_search_image_button"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:src="@drawable/ic_search_primary"
        android:background="@android:color/transparent"/>

</RelativeLayout>